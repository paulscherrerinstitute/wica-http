Note:

The keystore file is not kept in this project for security reasons.
Instead it should be provided by mounting an external volume.

The password for the keystore should be provided by overriding
as an argument to the Docker run command.
